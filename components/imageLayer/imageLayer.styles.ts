import styled from "styled-components";

export const ImageLayerSection = styled.section`
  display: flex;
  align-items: center;
  flex-direction: column;
  gap: 0;
`;

export const ImagerLayerLinkContainer = styled.div`
  display: flex;
  width: max-content;
  margin: 3em auto;
`;
