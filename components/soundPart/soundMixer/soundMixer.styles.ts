import styled from "styled-components";

export const SoundMixerContainer = styled.div`
  position: fixed;
  bottom: 0;
  width: 100vw;
  height: 400px;
  background-color: var(--primary-mixer);
  padding: 10px;
  display: flex;
  gap: 4px;
`;
